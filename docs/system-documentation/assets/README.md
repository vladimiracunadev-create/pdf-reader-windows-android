# Recursos de la documentación

Los diagramas editables se mantienen como bloques Mermaid dentro de los documentos Markdown para conservar una única fuente. El generador PDF los presenta como diagramas Mermaid identificados y legibles en tipografía monoespaciada. Los recursos gráficos reutilizados del producto permanecen en `docs/images/` y `assets/`.
