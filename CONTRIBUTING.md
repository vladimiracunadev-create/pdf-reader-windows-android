# Contributing

1. Crea una rama desde `main`.
2. Mantén el producto en modo **solo lectura** salvo cambio explícito de alcance.
3. Ejecuta `npm ci`, `npm run check` y `npm run build:pages`.
4. Abre PR usando la plantilla.
5. Espera los tres checks de CI en verde antes de merge.
