# Reglas del repositorio

- Usa exclusivamente `pnpm` para instalar dependencias y ejecutar scripts.
- No uses `npm` ni `npx`; usa `pnpm install`, `pnpm <script>` y `pnpm exec`.
- Mantén `pnpm-lock.yaml` actualizado y no agregues otros lockfiles de JavaScript.
